Работает на постгресе, таблицы создаются с помощью миграций. Подключение к бд надо настроить в application.yml
всего 4 метода:

GET localhost:8080/cars выводит все позиции. По умолчанию в порядке добавления, может содержать 
аргументы для сортировки. Например localhost:8080/cars?sort=number выводит отсортированные по номеру,
localhost:8080/cars?sort=years - по году выпуска

POST localhost:8080/cars добавляет новую позицию, описание в теле с помощью Json.
Пример: 
{
    "number": "KK831A66",
    "model": "Ford",
    "color": "blue",
    "year": "2017"
}

DELETE localhost:8080/cars/{number}
удаляет по номеру (номер - id)
пример localhost:8080/cars/BM312O96

GET localhost:8080/cars/info
выводит общее количество позиций в бд, время первого и последнего добавления.
    
